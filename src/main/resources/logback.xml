<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Аппендер для файла-->
    <appender name="FILE1" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>
                %d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n
            </Pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/archived/debug.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>
    <!-- Логгер для файла-->
    <logger name="ааа.Service" level="all" additivity="false">
        <appender-ref ref="FILE1"/>
    </logger>

    <!-- Аппендер для консоли-->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n
            </Pattern>
        </layout>
    </appender>
    <!-- Логгер для консоли-->
    <logger name="ааа.Lesson2" level="all" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Логгер для файла и консоли-->
    <logger name="ааа.WorkWithDB" level="all" additivity="false">
        <appender-ref ref="FILE-AUDIT"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <root level="all">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>